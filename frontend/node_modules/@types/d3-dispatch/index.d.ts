/*
  Temporary empty type definition to avoid TypeScript compilation issues
  with the d3-dispatch package
*/

declare module 'd3-dispatch' {
  export function dispatch(): any;
  const dispatch: any;
  export default dispatch;
}
